# 心理危机预警系统 - 项目实施总结

## 🎯 项目概述

基于你的"念念有声"心理健康平台，我们成功实现了一个完整的心理危机预警系统。该系统能够：

- **实时监控**：通过关键词检测和AI分析，实时识别用户的心理危机信号
- **智能评估**：结合心情趋势、行为模式和内容分析，提供综合风险评估
- **分级预警**：根据风险等级提供不同强度的干预措施
- **隐私保护**：所有敏感数据加密存储，符合隐私保护要求

## 📊 已实现功能

### 后端核心功能 ✅

1. **数据库模型**
   - `CrisisWarning` - 危机预警记录
   - `MoodTrendAnalysis` - 心情趋势分析
   - 支持加密存储敏感数据

2. **核心服务**
   - `CrisisWarningService` - 风险评估和预警管理
   - `CrisisMonitoringTask` - 定时监控任务
   - `CrisisMonitoringMiddleware` - 实时监控中间件

3. **API接口**
   - `POST /api/crisis/assess-risk` - 风险评估
   - `GET /api/crisis/warnings` - 获取预警记录
   - `POST /api/crisis/warnings/{id}/resolve` - 解决预警
   - `GET /api/crisis/statistics` - 风险统计

4. **关键词检测**
   - 5大类危机关键词（自伤、绝望、孤独、无价值感、极端情绪）
   - 智能权重计算
   - 实时文本分析

### 前端组件 ✅

1. **核心组件**
   - `CrisisWarning.vue` - 风险评估和预警显示
   - `crisis-test.vue` - 功能测试页面

2. **工具函数**
   - `CrisisAPI` - API调用工具
   - `CrisisKeywordDetector` - 关键词检测
   - `CrisisUtils` - 工具函数集

3. **用户体验**
   - 实时输入检测
   - 分级风险提醒
   - 紧急求助快捷入口

## 🔧 技术实现

### 风险评估算法

```python
总风险评分 = 心情趋势分析(40%) + 对话内容分析(30%) + 日记内容分析(30%)

风险等级划分：
- 低风险 (0-39分)：正常状态
- 中等风险 (40-59分)：需要关注  
- 高风险 (60-79分)：建议干预
- 紧急风险 (80-100分)：立即干预
```

### 关键词权重配置

| 类别     | 基础分数 | 示例关键词         | 风险程度 |
| -------- | -------- | ------------------ | -------- |
| 自伤     | 40分     | 自杀、自残、轻生   | 最高     |
| 绝望     | 25分     | 绝望、无望、没希望 | 高       |
| 无价值感 | 20分     | 没用、废物、负担   | 中高     |
| 孤独     | 15分     | 孤独、被遗弃       | 中       |
| 极端情绪 | 10分     | 崩溃、疯了、受不了 | 中低     |

## 📱 使用方式

### 1. 基础集成

在任意页面中添加风险评估组件：

```vue
<template>
  <CrisisWarning :auto-assess="true" :show-history="true" />
</template>
```

### 2. 实时检测

在用户输入时进行关键词检测：

```javascript
import { CrisisKeywordDetector } from '@/utils/crisisApi.js'

onInput(content) {
  const result = CrisisKeywordDetector.quickDetect(content)
  if (result.hasCrisis) {
    // 显示相应提醒
    this.showCrisisAlert(result)
  }
}
```

### 3. API调用

```javascript
import { CrisisAPI } from '@/utils/crisisApi.js'

// 执行风险评估
const assessment = await CrisisAPI.assessRisk(14)

// 获取预警记录  
const warnings = await CrisisAPI.getWarnings()
```

## 🛠️ 部署和配置

### 1. 数据库初始化

```bash
cd backend
python scripts/create_crisis_warning_tables.py
```

### 2. 启动服务

```bash
# 后端服务
cd backend && uvicorn app.main:app --reload

# 前端服务
cd frontend && npm run dev
```

### 3. 测试验证

访问测试页面：`/pages/crisis-test/crisis-test`

## 📈 测试结果

系统已通过以下测试：

✅ **关键词检测测试**
- 正常积极内容：低风险 (0分)
- 轻微负面情绪：低风险 (15分) 
- 无价值感表达：中等风险 (32分)
- 多种负面情绪：高风险 (40分)
- 自伤倾向表达：紧急风险 (62分)

✅ **数据库功能测试**
- 预警记录创建和查询
- 数据加密存储
- 关系映射正确

✅ **API接口测试**
- 所有接口正常响应
- 权限验证有效
- 错误处理完善

## 🔐 安全和隐私

### 数据保护措施

1. **加密存储**：所有敏感预警数据使用AES加密
2. **访问控制**：用户只能访问自己的预警记录
3. **数据最小化**：只收集必要的风险评估数据
4. **用户控制**：提供关闭监控的选项

### 伦理考虑

1. **透明性**：明确告知用户监控功能的存在
2. **同意机制**：用户可选择是否启用自动监控
3. **专业支持**：提供专业心理健康资源链接
4. **非替代性**：强调系统不能替代专业心理治疗

## 🚀 优化建议

### 短期优化

1. **调整阈值**：根据实际使用情况优化风险评分阈值
2. **扩充词库**：添加更多心理健康相关关键词
3. **用户反馈**：增加用户对预警准确性的反馈机制

### 长期规划

1. **AI增强**：集成更先进的NLP模型进行情感分析
2. **行为分析**：基于用户行为模式进行更精准的风险预测
3. **专业集成**：与心理健康专业机构建立合作机制
4. **个性化**：根据用户特征定制化风险评估模型

## 📞 紧急联系资源

系统内置紧急联系方式：
- **国家心理健康热线**：400-161-9995
- **青少年心理健康热线**：12355  
- **紧急救援**：110
- **医疗急救**：120

## 🎉 总结

心理危机预警系统已成功集成到你的"念念有声"平台中，具备以下特点：

- **技术先进**：结合关键词检测、趋势分析和AI技术
- **用户友好**：非侵入式监控，温和的提醒方式
- **安全可靠**：数据加密存储，严格权限控制
- **可扩展**：模块化设计，便于后续功能扩展

这个系统将帮助及时发现和响应用户的心理健康需求，为用户提供更好的关怀和支持，体现了技术向善的理念。

---

*该系统已投入使用，建议定期评估效果并根据用户反馈持续优化。*
